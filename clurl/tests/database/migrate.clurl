# ============================================================
# Database Management Tests
# Tests for migrate and wipe commands
# ============================================================

# --- Test migrate command ---
RUN cargo run --quiet -p chickie-cli -- migrate

[Asserts]
exit-code == 0
stdout contains "Migrações aplicadas com sucesso"
